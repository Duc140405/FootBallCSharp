<Window x:Class="Football_Management_System.MatchResultWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Kết quả trận đấu" 
        Height="500" 
        Width="800"
        WindowStartupLocation="CenterScreen">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="50"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        
        <Border Grid.Row="0" Background="#1565C0">
            <TextBlock Text="KẾT QUẢ TRẬN ĐẤU" 
                       Foreground="White" 
                       FontSize="20" 
                       FontWeight="Bold"
                       VerticalAlignment="Center"
                       Margin="20,0,0,0"/>
        </Border>

        
        <Grid Grid.Row="1" Margin="10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="300"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

     
            <StackPanel Grid.Column="0" Margin="10">
                
                <TextBlock Text="Chọn Trận Đấu" Margin="0,0,0,3"/>
                <ComboBox x:Name="cboMatch" 
                          Height="28"
                          SelectionChanged="cboMatch_SelectionChanged"/>

                <TextBlock Text="Đội Nhà" Margin="0,10,0,3"/>
                <TextBox x:Name="txtHomeTeam" Height="28"/>

                <TextBlock Text="Đội Khách" Margin="0,10,0,3"/>
                <TextBox x:Name="txtAwayTeam" Height="28"/>

                <TextBlock Text="Ngày Thi Đấu" Margin="0,10,0,3"/>
                <DatePicker x:Name="dpMatchDate" Height="28"/>

                <Grid Margin="0,10,0,0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    
                    <StackPanel Grid.Column="0" Margin="0,0,5,0">
                        <TextBlock Text="Tỷ Số Đội Nhà" Margin="0,0,0,3"/>
                        <TextBox x:Name="txtHomeScore" Height="28"/>
                    </StackPanel>
                    
                    <StackPanel Grid.Column="1" Margin="5,0,0,0">
                        <TextBlock Text="Tỷ Số Đội Khách" Margin="0,0,0,3"/>
                        <TextBox x:Name="txtAwayScore" Height="28"/>
                    </StackPanel>
                </Grid>

                
                <StackPanel Orientation="Horizontal" Margin="0,20,0,0">
                    <Button x:Name="btnAdd" Content="Thêm" 
                            Width="60" Height="32" 
                            Background="#2196F3" Foreground="White"
                            Margin="0,0,8,0"
                            Click="btnAdd_Click"/>
                    <Button x:Name="btnEdit" Content="Sửa" 
                            Width="60" Height="32" 
                            Background="#FF9800" Foreground="White"
                            Margin="0,0,8,0"
                            Click="btnEdit_Click"/>
                    <Button x:Name="btnDelete" Content="Xóa" 
                            Width="60" Height="32"
                            Background="#f44336" Foreground="White"
                            Margin="0,0,8,0"
                            Click="btnDelete_Click"/>
                    <Button x:Name="btnClear" Content="Làm mới" 
                            Width="70" Height="32"
                            Background="#607D8B" Foreground="White"
                            Click="btnClear_Click"/>
                </StackPanel>

             
                <TextBlock x:Name="txtStatus" 
                           Foreground="#1565C0" 
                           FontWeight="Bold"
                           TextWrapping="Wrap"
                           Margin="0,15,0,0"/>
            </StackPanel>

            
            <DataGrid x:Name="dgMatches" 
                      Grid.Column="1" 
                      AutoGenerateColumns="False"
                      IsReadOnly="True"
                      Margin="10"
                      SelectionChanged="dgMatches_SelectionChanged">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Đội Nhà" Binding="{Binding HomeTeam}" Width="100"/>
                    <DataGridTextColumn Header="Đội Khách" Binding="{Binding AwayTeam}" Width="100"/>
                    <DataGridTextColumn Header="Ngày" Binding="{Binding MatchDate, StringFormat=dd/MM/yyyy}" Width="90"/>
                    <DataGridTextColumn Header="Kết Quả" Binding="{Binding Result}" Width="80"/>
                </DataGrid.Columns>
            </DataGrid>
        </Grid>
    </Grid>
</Window>
